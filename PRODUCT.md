# Product

## Register

product

## Users

Shopify 运营人员、主题开发者和页面搭建人员。他们需要快速查看公开产品页的模块结构，并复制单个模块用于分析、迁移或重建。

## Product Purpose

从公开 Shopify 产品页的 `#MainContent` 中提取一级 `section` 和 `div`，按原始顺序分组，并提供接近来源页面的静态预览、源码复制和下载。用户可以排除不需要的模块、批量替换产品文案，并通过 DeepSeek 将选中模块改写为可直接粘贴到 Shopify “Custom Liquid”模块中的独立代码。成功意味着模块边界清楚、资源地址可用、预览布局与来源一致，AI 用量可控，生成结果可验证且不会污染主题全局样式。

## Brand Personality

可靠、克制、直接。界面以任务为中心，优先展示来源、模块边界、预览状态和复制操作。

## Anti-references

避免装饰性后台界面、无法解释的视觉变化、把渲染后 HTML 误称为 Liquid 源码，以及在预览宽度变化时不提示用户却静默改变模块排版。

## Design Principles

1. 忠实度优先。预览必须携带来源页面的视口、样式和根节点上下文。
2. 边界明确。每个模块的标签、ID、Class 和顺序都可核对。
3. 安全默认。原始预览不执行来源脚本；生成代码只在无同源权限的隔离 iframe 中执行，并阻止外部脚本和网络型 JavaScript。
4. 复制可预期。区分纯 HTML 与包含样式依赖的版本。
5. 问题可见。资源加载失败、页面限制和静态预览差异不能被隐藏。
6. 成本可控。排除模块后不发送给 AI；替换规则变化后明确标记旧结果，避免误用和无意识重复生成。

## Accessibility & Inclusion

保持键盘可操作、清晰焦点、可读对比度和移动端结构化布局。加载动效尊重 reduced motion。状态信息不能只依赖颜色表达。
