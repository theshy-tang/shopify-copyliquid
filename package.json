{
  "name": "shopify-section-copier",
  "version": "1.0.0",
  "private": true,
  "description": "Extract Shopify MainContent modules and convert them to Custom Liquid with DeepSeek.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --check server.js && node --check public/app.js && node --check lib/custom-liquid.js && node --test"
  },
  "dependencies": {
    "@sparticuz/chromium": "^149.0.0",
    "cheerio": "^1.2.0",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "playwright-core": "^1.53.2"
  }
}
